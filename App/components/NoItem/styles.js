import {StyleSheet} from 'react-native';
import {font} from '../../global';

const styles = StyleSheet.create({
  center: {textAlign: 'center', paddingTop: '30%', fontFamily: font},
});

export default styles;
